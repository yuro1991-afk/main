[project]
name = "genesis-world-canon"
version = "0.1.0"
description = "Genesis world canon"
requires-python = ">=3.11"
[project.scripts]
genesis-world-canon = "world_canon:main"
[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[tool.setuptools.package-dir]
"" = "python"
