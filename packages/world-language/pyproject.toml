[project]
name = "genesis-world-language"
version = "0.1.0"
description = "Genesis Python world language"
requires-python = ">=3.11"
[project.scripts]
genesis-world-language = "world_language:main"
[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[tool.setuptools.package-dir]
"" = "python"
