[project]
name = "genesis-python-infra"
version = "0.1.0"
description = "Genesis Python infrastructure"
requires-python = ">=3.11"
[project.scripts]
genesis-python-infra = "python_infra:main"
[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[tool.setuptools.package-dir]
"" = "python"
