[project]
name = "genesis-world-3d-sound"
version = "0.1.0"
description = "Genesis World 3D sound engine"
requires-python = ">=3.11"
[project.scripts]
genesis-world-3d-sound = "world_3d_sound:main"
[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[tool.setuptools.package-dir]
"" = "python"
