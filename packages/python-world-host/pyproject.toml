[project]
name = "genesis-python-world-host"
version = "0.1.0"
description = "Genesis Python world host"
requires-python = ">=3.11"
[project.scripts]
genesis-python-world-host = "python_world_host:main"
[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[tool.setuptools.package-dir]
"" = "python"
