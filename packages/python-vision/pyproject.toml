[project]
name = "genesis-python-vision"
version = "0.1.0"
description = "Genesis Python vision"
requires-python = ">=3.11"
[project.scripts]
genesis-python-vision = "python_vision:main"
[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[tool.setuptools.package-dir]
"" = "python"
