[project]
name = "genesis-python-ears"
version = "0.1.0"
description = "Genesis Python agent ears"
requires-python = ">=3.11"
[project.scripts]
genesis-python-ears = "python_ears:main"
[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[tool.setuptools.package-dir]
"" = "python"
