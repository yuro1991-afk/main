[project]
name = "genesis-agent-support"
version = "0.1.0"
description = "Genesis agent support"
requires-python = ">=3.11"
[project.scripts]
genesis-agent-support = "agent_support:main"
[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[tool.setuptools.package-dir]
"" = "python"
