[project]
name = "genesis-python-eyes"
version = "0.1.0"
description = "Genesis Python agent eyes"
requires-python = ">=3.11"
[project.scripts]
genesis-python-eyes = "python_eyes:main"
[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[tool.setuptools.package-dir]
"" = "python"
