[project]
name = "genesis-python-suit"
version = "0.1.0"
description = "Genesis Python agent suit"
requires-python = ">=3.11"
[project.scripts]
genesis-python-suit = "python_suit:main"
[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[tool.setuptools.package-dir]
"" = "python"
