[project]
name = "genesis-python-world-knowledge"
version = "0.1.0"
description = "Genesis Python world knowledge"
requires-python = ">=3.11"
[project.scripts]
genesis-python-world-knowledge = "python_world_knowledge:main"
[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[tool.setuptools.package-dir]
"" = "python"
