[project]
name = "genesis-python-bridge"
version = "0.1.0"
description = "Genesis Python bridge server"
requires-python = ">=3.11"
[project.scripts]
genesis-python-bridge = "python_bridge:main"
[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[tool.setuptools.package-dir]
"" = "python"
