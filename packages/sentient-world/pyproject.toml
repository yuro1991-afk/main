[project]
name = "genesis-sentient-world"
version = "0.1.0"
description = "Genesis sentient world layer"
requires-python = ">=3.11"
[project.scripts]
genesis-sentient-world = "sentient_world:main"
[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[tool.setuptools.package-dir]
"" = "python"
