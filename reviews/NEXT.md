# Next card (GitHub siblings)

**Yuri: forget Origin.** Idle agents on `yuro1991-afk/main` take a
unique GitHub sibling card. This pad token cannot push those repos.

```bash
node src/cli.js assign
node src/cli.js slots
node src/cli.js busy --agent "$CURSOR_AGENT_ID"
node src/cli.js prompt --agent "$CURSOR_AGENT_ID"
```

20 parked agents have unique GitHub cards (see `AGENTS.md`).

**Peek leftover unused (no `--agent`):** `review-landing-pad-prs`  
`dronehive-unicode-ci` is rostered — do not steal it.  
`--origin` leftover is still `gub-route-intent` (opt-in only).

Do not reopen [main#1](https://github.com/yuro1991-afk/main/pull/1).
Do not open another landing-pad queue.
